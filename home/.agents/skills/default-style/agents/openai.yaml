interface:
  display_name: "Default Style"
  short_description: "Shared default response profile"
  default_prompt: "Use $default-style to answer in the shared summary-first, dashboard-first communication style."

